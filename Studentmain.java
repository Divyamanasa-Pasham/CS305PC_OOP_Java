package Student;
import java.util.Scanner;

class Studentmain implement Operation{
	static string id,name,branch,password;
	static fname=*Student.txt";
	static Scanner s=new Scanner(System.in);
	
	public void registraction(
	
	}
	public void logIn(string id,String password){
	try{
	BufferReader reader=new BufferReader(new Filename(frame));
	String data;
	while(data=reader.readline()){
		String
		
		}
	public void exit(){
}
